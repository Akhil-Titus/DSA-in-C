cmake_minimum_required(VERSION 3.5)
project(linked-list)

# set(SOURCE_FILES
#     main.c
#     linked-list.c)

# add_executable(${PROJECT_NAME} ${SOURCE_FILES})

add_executable("linked-list" "main.c" "linked-list.c")